#!/usr/bin/env python3
import os
import requests
from requests.auth import HTTPBasicAuth
  
from dotenv import load_dotenv
load_dotenv()

# TODO: We dont have protected enpoints yet, but they will go here
